# KPI 考核管理系统 需求符合度清单

> os-project-dev-issue 步骤 4.5:逐条对照需求 V1.3,标 ✅ 完整实现 / ⚠️ 有偏差 / ❌ 未实现;
> 非 ✅ 条目只有三个出口(退回细化 / 平台受限挂起 / 升级裁决),没有「先简化实现」。
> 证据列指向代码与自测记录(`docs/05-自测记录.md`,用例编号 T-xx)。

## 一、核心业务流程(需求 §3)

| # | 需求 | 态 | 实现落点 / 证据 | 偏差与出口 |
|---|---|---|---|---|
| 1 | 配置方案:新建版本 → 选指标 → 分权重与目标 → 配考核关系与参与范围 | ✅ | `kpi_plan` + 主从 `kpi_plan_step / kpi_plan_subject / kpi_plan_indicator / kpi_staff_assignment / kpi_personal_item`,内联网格录入;T1、T2 | — |
| 2 | 发布前完整性检查 | ✅ | `PlanPublishHook`:流程节点合法、主体非空、每主体有指标、权重合计 100%、目标值齐、争议关闭,逐条列出问题;T3 | — |
| 3 | 发布:冻结参与范围、流程与口径,生成指标下达与到人分工,发起流程 | ✅ | 发布后子配置冻结(`PlanChildFreezeHook`,T7);为每个主体生成填报单与明细并冻结目标 / 权重 / 计分方式副本(T5、T6);到人分工随方案配置,发布后冻结 | 「发起流程」= 生成填报单进入「填报中」,无站内通知(需求未要求通知) |
| 4 | 填报:按填报视图录入或批量导入 → 自动保存 → 即时算分 → 提交冻结 | ✅ | 填报单表单内嵌明细网格 + 明细列表内联编辑;`EntryLineScoreHook` 保存即算分(T8、T9、T11);提交后明细冻结(T16);导入走平台 CSV 向导、xlsx 导出 | ⚠️ xlsx 直接导入为平台导入 API 能力,未做专用按钮(pre-intake R-低-02,列二期) |
| 5 | 核对与审核:分公司并行核对 → 人力审核 → 领导审批;驳回填原因、退回上一环节 | ✅ | 提交按分公司生成并行核对任务(T15),争议阻断推进(T18),全部确认自动推进(T19);驳回原因必填(T20),退回上一节点并重置核对(T21);人力 → 领导 → 已通过(T22、T23) | — |
| 6 | 计分:按既定口径自动算每个指标得分 | ✅ | `src/lib/scoring.ts` 四种方式 + 单元测试 14 条;计算说明落库 | 口径细节(逆向指标、封顶、到人合成)按推断实现,待疑点 Q-01~Q-05 拍板后只改数据 / 一处代码 |
| 7 | 汇总:部门 / 分公司 / 到人 / 分管领导四维,可钻取到指标得分明细 | ✅ | `kpi_result` 由 `results-service` 在通过 / 归档 / 调整落地时生成(T24、T27);加减分审批落定后由 `BonusAfterDecideHook` 立即重算(T48、T49);结果对象带来源填报单与计算明细 JSON;看板 2、矩阵报表可钻取 | — |
| 8 | 归档:审批通过后生成不可变快照,数据锁定 | ✅ | `kpi_snapshot`(SHA-256 校验和)+ `SnapshotImmutableHook`(T28~T30);归档后明细、加减分锁定(T31、T32) | — |

## 二、范围功能(需求 §1.3)

| # | 需求 | 态 | 实现落点 / 证据 | 偏差与出口 |
|---|---|---|---|---|
| 9 | 指标管理(定义、来源、计分方式、权重、版本可追溯) | ✅ | `kpi_indicator` + `kpi_indicator_step`;字段历史跟踪;版本号 + 替代版本;状态机 | — |
| 10 | 考核方案多版本并存、发布后冻结、复制上一版 | ✅ | `kpi_plan` 状态机;`PlanCloneHook` 复制节点 / 主体 / 下达 / 分工(T35) | 「多版本并存」按疑点 Q-09 的 B 理解(同周期草稿与已发布并存) |
| 11 | 流程可配置、随方案版本冻结 | ✅ | `kpi_plan_step` 数据化节点;两节点流程验证(T36) | 配置粒度按疑点 Q-08 的 B(可增删节点、改审核岗位);不同部门走不同流程未支持(Q-08 C) |
| 12 | 加减分 | ✅ | `kpi_bonus` 主从填报单,审批后计入最终得分(T25) | 封顶 / 保底未做(疑点 Q-04 待拍板) |
| 13 | 数据调整(源数据 / 计算结果) | ✅ | `kpi_adjustment`:记录调整前值、审批、批准即落地并重算(T26、T27) | — |
| 14 | 审计查询 | ✅ | `kpi_review_record` 只增不改(T33、T34)+ 平台审计日志 / 记录历史页签 | — |
| 15 | 组织与权限 | ✅ | 复用平台组织单元 / 用户;岗位 6、权限集 6;共享规则不再是元数据,由方案发布按参与主体动态写入(`src/services/sharing-service.ts`,T39) | 见 §三 数据范围偏差 |
| 16 | 数据接入与归档 | ✅ | 填报 / 导入 / 系统计算三类来源在指标上声明;归档快照 | 「系统计算」来源的自动取数需对接客户数据源,零外部集成(蓝图 §4) |

## 三、角色与安全(需求 §2)

| # | 需求 | 态 | 实现落点 / 证据 | 偏差与出口 |
|---|---|---|---|---|
| 17 | 六类角色职责 | ✅ | 岗位 `kpi_admin / kpi_hr_reviewer / kpi_hr_head / kpi_dept_reporter / kpi_branch_checker / kpi_exec_leader`,流程节点按岗位把关(hook 经 `sys_user_position` 查询) | 组织 owner / admin 自动等同系统管理员 |
| 18 | 数据范围:全部 / 分管 / 本部门 / 仅本人 | ⚠️ | 全部 = org、仅本人 = own 原生;本部门 / 本分公司 / 分管范围 = private 基线 + 方案发布时按参与主体与分管领导写入的动态共享规则(`src/services/sharing-service.ts`);到人结果行级安全只看本人 | **平台受限(新)**:分管领导一路(收件方为具体用户)已实测生效(T46、T47);单元一路在演示种子组织上展开不出人 —— 共享规则被引擎盖上当前组织,而种子写入的 `sys_business_unit` 行 `organization_id` 为空,收件方展开对二者做**等值**比较(平台自己的租户过滤是「本组织或为空」)。已上报 objectstack-ai/objectstack#14547;管理员在 Setup 里新建的组织单元不受影响。不再依赖企业版 `hierarchy-security` |
| 19 | 越权访问拒绝、经办人以登录账号为准 | ✅ | 权限集对象级 + 字段级;经办人 / 审批人字段只读由 hook 盖章 | — |
| 20 | 账号安全(密码策略、停用即失效、失败限制、白名单、敏感操作留痕) | ✅ | 平台账号体系(`sys_user` 失败计数 / 锁定 / 封禁 / 强制改密);审批、驳回、归档留痕 | 访问来源白名单归部署层(反向代理 / 网关)配置 |

## 四、非功能(需求 §4)

| # | 需求 | 态 | 实现落点 / 证据 | 偏差与出口 |
|---|---|---|---|---|
| 21 | 得分口径唯一、可重复计算、历史不漂移 | ✅ | 计分只在 `scoring.ts`;明细冻结指标口径副本;快照不可变 | — |
| 22 | 遵循 Excel 习惯,即时出分,校验提示,逐层钻取 | ⚠️ | 内联网格、即时算分、三段式提示、报表钻取 | 自由公式 / 跨 sheet 引用不进系统(范围边界,R-高-02);需业务方演示确认 |
| 23 | 性能:大数据量表格可正常浏览编辑 | ⚠️ | 填报单按主体拆分(每表数十行),列表分页 | 未做压测(升级项:实施期用真实数据量验证) |
| 24 | 可扩展:新增计分口径 / 考核关系 / 视图通过配置实现 | ✅ | 计分方式数据化(阶梯 / 区间 / 公式);考核关系与流程节点是方案数据;视图为元数据 | 新计分「方式」需加代码分支(口径参数不需要) |

## 五、统计

- ✅ 完整实现 20 条;⚠️ 有偏差 4 条(#4 xlsx 直导、#18 数据范围的单元一路、#22 自由公式边界、#23 未压测);❌ 未实现 0 条。
- ⚠️ 出口:#18 平台受限(已上报 objectstack-ai/objectstack#14547,待平台修复后复测,不改本应用代码);#4 / #23 升级为二期事项交 PM 裁决;#22 退回需求确认(演示后拍板)。
