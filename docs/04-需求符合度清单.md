# KPI 考核管理系统 需求符合度清单

> os-project-dev-issue 步骤 4.5:逐条对照**《设计方案》V1.0**(`docs/00-设计方案.md`,客户方
> 2026-09-02 确认第 10 章全部 17 项 —— 第 1~12 项同意产品默认,第 13 / 14 / 15 项选 A),
> 标 ✅ 完整实现 / ⚠️ 有偏差 / ❌ 未实现;非 ✅ 条目只有三个出口(退回细化 / 平台受限挂起 /
> 升级裁决),没有「先简化实现」。需求条目沿用需求 V1.3 的编号,口径以设计方案为准。
> 章节引用一律指向设计方案(如「设计 5.2」「第 10 章第 10 项」「表 10」),不再引用疑点编号。
> 证据列指向代码与自测记录(`docs/05-自测记录.md`,用例编号 T-xx;每个编号都能在该文件 §3 总表查到)。

## 一、核心业务流程(需求 §3 / 设计第 4 章)

| # | 需求 | 态 | 实现落点 / 证据 | 偏差与出口 |
|---|---|---|---|---|
| 1 | 配置方案:新建版本 → 选指标 → 分权重与目标 → 配考核关系与参与范围 | ✅ | `kpi_plan` + 主从 `kpi_plan_step / kpi_plan_subject / kpi_plan_indicator / kpi_staff_assignment / kpi_personal_item`,内联网格录入(设计 5.2);T1、T2 | — |
| 2 | 发布前完整性检查 | ✅ | `PlanPublishHook`:流程节点合法、主体非空、每主体有指标、权重合计 100%、目标值齐、争议关闭,逐条列出问题;T3 | — |
| 3 | 发布:冻结参与范围、流程与口径,生成指标下达与到人分工,发起流程 | ✅ | 发布后子配置冻结(`PlanChildFreezeHook`,T7);为每个主体生成填报单与明细并冻结目标 / 权重 / 计分方式副本(T5、T6);到人分工随方案配置,发布后冻结 | 「发起流程」= 生成填报单进入「填报中」;站内通知与到期提醒在设计表 10 列为二期候选 |
| 4 | 填报:按填报视图录入或批量导入 → 自动保存 → 即时算分 → 提交冻结 | ✅ | 填报单表单内嵌明细网格 + 明细列表内联编辑;`EntryLineScoreHook` 保存即算分(T8、T9、T11);提交后明细冻结(T16);导入走平台 CSV 向导、xlsx 导出 | xlsx **直接导入按钮**按设计表 10 列为二期候选(现走平台通用导入),不计为本期偏差 |
| 5 | 核对与审核:分公司并行核对 → 人力审核 → 领导审批;驳回填原因、退回上一环节 | ✅ | 提交按分公司生成并行核对任务(T15),争议阻断推进(T18),全部确认自动推进(T19);驳回原因必填(T20),退回上一节点并重置核对(T21,设计表 7 第 8 项);人力 → 领导 → 已通过(T22、T23) | — |
| 6 | 计分:按既定口径自动算每个指标得分 | ✅ | `src/lib/scoring.ts` 四种方式 + 单元测试 14 条;计算说明落库;逆向指标完成率 = 目标 ÷ 实际(第 10 章第 4 项)、个人得分 = 板块折算 + 承接项相加不封顶(第 5 项)、计分方式清单四种(第 12 项),`src/lib/aggregate.ts` 顶部注释已改引设计第 8 章表 6;T8、T9、T11、T37 | — 口径已由第 10 章第 4 / 5 / 12 项确认,无偏差 |
| 7 | 汇总:部门 / 分公司 / 到人 / 分管领导四维,可钻取到指标得分明细 | ✅ | `kpi_result` 由 `results-service` 在通过 / 归档 / 调整落地时生成(T24、T27);加减分审批落定后由 `BonusAfterDecideHook` 立即重算(T48、T49、T60);计算明细 JSON 逐指标带得分率、得分与「已调整」标记(T63);看板 2、矩阵报表可钻取 | — |
| 8 | 归档:审批通过后生成不可变快照,数据锁定 | ✅ | `kpi_snapshot`(SHA-256 校验和)+ `SnapshotImmutableHook`(T28~T30);归档后明细、加减分锁定(T31、T32);归档不可改、下一周期修正(第 10 章第 11 项) | — |

## 二、范围功能(需求 §1.3 / 设计第 5 章)

| # | 需求 | 态 | 实现落点 / 证据 | 偏差与出口 |
|---|---|---|---|---|
| 9 | 指标管理(定义、来源、计分方式、权重、版本可追溯) | ✅ | `kpi_indicator` + `kpi_indicator_step`(设计 5.1);字段历史跟踪;版本号 + 替代版本;状态机 | — |
| 10 | 考核方案多版本并存、发布后冻结、复制上一版 | ✅ | `kpi_plan` 状态机;`PlanCloneHook` 复制节点 / 主体 / 下达 / 分工(T35);同周期唯一生效版本见 #26 | — 「草稿与已发布并存、同周期只有一个生效版本」= 第 10 章第 10 项确认口径,已按此实现 |
| 11 | 流程可配置、随方案版本冻结 | ✅ | `kpi_plan_step` 数据化节点;两节点流程验证(T36) | — 粒度 = 可增删节点、可改审核岗位(第 10 章第 9 项);「不同部门走不同流程」在设计表 10 列为二期候选 |
| 12 | 加减分 | ✅ | `kpi_bonus` 主从填报单,审批后计入最终得分(T25、T60);**提出 = 人力审核、审批 = 人力负责人**(第 10 章第 6 项,本工作项落地:`src/hooks/bonus.hook.ts` 的 `requiredBonusPosition`;真实岗位账号取证 T65~T68,反例 T59) | — 不封顶不保底 = 第 10 章第 6 项确认口径;两个人力岗位账号都已能亲自操作(T65~T67),部门填报人员的新建权限已同步收窄(T58) |
| 13 | 数据调整(源数据 / 计算结果) | ✅ | `kpi_adjustment`:记录调整前值、审批、批准即落地并重算(T26、T27);落地标记见 #25(设计 5.7) | — |
| 14 | 审计查询 | ✅ | `kpi_review_record` 只增不改(T33、T34)+ 平台审计日志 / 记录历史页签(设计 5.9) | — |
| 15 | 组织与权限 | ✅ | 复用平台组织单元 / 用户;岗位 6、权限集 6;共享规则不再是元数据,由方案发布按参与主体动态写入(`src/services/sharing-service.ts`,T39) | 见 §三 #17、#18 |
| 16 | 数据接入与归档 | ✅ | 填报 / 导入 / 系统计算三类来源在指标上声明;归档快照 | 「系统计算」来源的自动取数需对接客户数据源,零外部集成;Excel 自由公式与跨表引用不进系统 = 第 16 项确认的范围边界 |
| 25 | 结果调整改某指标得分并标记「已调整」(设计 5.7、第 10 章第 14 项 = A) | ✅ | `kpi_entry_line.is_adjusted` / `adjust_type_applied`(只读,由 `AdjustmentHook` 以系统上下文写入,源数据与结果调整都标);列入填报明细列表视图与填报单内嵌网格;结果计算明细 JSON 每条指标带 `is_adjusted`;T62、T63 | — 网格列为**显式声明**(显式声明会整体替换平台派生的列集):19 列 = 子对象除主从字段外的全部字段,顺序沿用字段定义顺序、两个新列排最后,计算说明 / 最近调整 / 指标三列用 `defaultHidden` 折叠而非删除,派生列一列未少(评审 🟡-1,见 `docs/05` 问题记录 #4) |
| 26 | 同一周期只有一个生效版本(设计 5.2、第 10 章第 10 项) | ✅ | `PlanPublishHook` 发布前校验同 `period_type + year + period_no` 是否已有「已发布」方案,已关闭 / 已归档不阻断;拒绝文案点名生效版本;候选只按状态取、租户判定在纯函数里做(组织为空的种子 / 导入方案同样算生效版本,T71 + 消融对照);T61、T71 + 单元测试 4 条 | — |

## 三、角色与安全(需求 §2 / 设计第 9 章)

| # | 需求 | 态 | 实现落点 / 证据 | 偏差与出口 |
|---|---|---|---|---|
| 17 | 六类角色职责 | ✅ | 岗位 `kpi_admin / kpi_hr_reviewer / kpi_hr_head / kpi_dept_reporter / kpi_branch_checker / kpi_exec_leader`,流程节点按岗位把关(hook 经 `sys_user_position` 查询);加减分登记 / 审批的岗位分离已落地(T59、T66);**人力岗位的记录级写范围**由方案发布写入的岗位共享规则(收件方类型 = `position`)授予 —— 人力审核登记加减分(T65)、人力负责人审批(T67)、人力审核在本节点驳回与审核通过(T69)均由真实岗位账号跑通,方案关闭后同样降为只读(T70) | — 第一轮曾因人力岗位无记录级写权限记为偏差,已按调度员 2026-09-02 的裁定(选项 A)修复并重测,详见 `docs/05-自测记录.md` 问题记录 #1 |
| 18 | 数据范围:全部 / 分管 / 本部门 / 仅本人 | ✅ | 全部 = org、仅本人 = own 原生;本部门 / 本分公司 / 分管范围 = private 基线 + 方案发布时按参与主体、**人力岗位**、分管领导、到人分工写入的**动态共享规则**(`src/services/sharing-service.ts`),组织变化零元数据改动;到人结果行级安全只挡他人的到人维度(T41、T44、T46、T51)。不再依赖企业版 `hierarchy-security`(设计第 17 项) | 四点实现说明:① 规则**按方案**建(条件带方案 id、规则名按方案加前缀),收件方三类:组织单元(`unit_and_subordinates`)、岗位(`position`,人力审核 / 人力负责人对本方案填报单与数据调整可编辑)、个人(`user`),发布时对账停用本方案下已不该存在的规则 —— 换掉分管领导、撤主体、撤到人分工都当场失效,历史方案的授权原样保留(T54~T56);② 方案**已关闭 / 已归档**后,填报单 / 核对任务 / 调整由 `edit` 降为 `read`(留读、去写),结果本就只读(T57);③ 本应用的业务记录全部由 hook 以系统上下文创建,而平台对 `isSystem` 写入跳过记录共享物化(官方补偿手段是重新求值规则),因此每个业务触发点都重新声明一次相关规则;④ 演示种子的组织单元 `organization_id` 为空,`src/data/align-demo-units.ts` 在启动时把它们补成与 Setup 新建单元一致 —— 这是 objectstack-ai/objectstack#14547 的**临时夹具**修补,平台修复后删除(T52) |
| 19 | 越权访问拒绝、经办人以登录账号为准 | ✅ | 权限集对象级 + 字段级;经办人 / 审批人字段只读由 hook 盖章;越权反例 T42、T58、T59、T64、T66、T68 | — |
| 20 | 账号安全(密码策略、停用即失效、失败限制、白名单、敏感操作留痕) | ✅ | 平台账号体系(`sys_user` 失败计数 / 锁定 / 封禁 / 强制改密);审批、驳回、归档留痕 | 访问来源白名单归部署层(反向代理 / 网关)配置 |
| 27 | 分公司核对只能确认 / 提出争议,不能改值(第 10 章第 13 项 = A) | ✅ | 核对人员权限集对填报明细只有读 / 导出,改值在权限层即被拒(T64);确认与争议走 `kpi_check_task`(T45、T17);改值由填报部门经数据调整流程完成并留痕 | — |

## 四、非功能(需求 §4)

| # | 需求 | 态 | 实现落点 / 证据 | 偏差与出口 |
|---|---|---|---|---|
| 21 | 得分口径唯一、可重复计算、历史不漂移 | ✅ | 计分只在 `scoring.ts`、汇总只在 `aggregate.ts`(口径依据设计第 8 章表 6);明细冻结指标口径副本;快照不可变 | — |
| 22 | 遵循 Excel 习惯,即时出分,校验提示,逐层钻取 | ✅ | 内联网格、即时算分、三段式提示、报表钻取 | 自由公式 / 跨 sheet 引用不进系统 —— 第 16 项已确认为范围边界,不再计为偏差 |
| 23 | 性能:大数据量表格可正常浏览编辑 | ⚠️ | 填报单按主体拆分(每表数十行),列表分页 | **出口:升级** —— 实施期用真实数据量验证,见设计表 10「试运行」阶段(一个考核周期真实跑一遍,值守与缺陷修复) |
| 24 | 可扩展:新增计分口径 / 考核关系 / 视图通过配置实现 | ✅ | 计分方式数据化(阶梯 / 区间 / 公式);考核关系与流程节点是方案数据;视图为元数据 | 新计分「方式」需加代码分支(口径参数不需要);清单四种 = 第 12 项确认 |

## 五、统计

- 共 27 条:✅ 完整实现 **26** 条;⚠️ 有偏差 **1** 条(#23 未压测);❌ 未实现 **0** 条。
- ⚠️ 出口:
  - **#23 大数据量性能** → 升级:实施期真实数据量验证,见设计表 10「试运行」阶段。
- 已闭环:#17 第一轮的偏差(人力岗位无记录级写权限)按调度员 2026-09-02 裁定的选项 A 修复,并以真实岗位账号重测通过(T65~T70),故本轮改 ✅。
- 说明:#18 已完整实现,但其中的演示夹具对齐是临时的,随 objectstack-ai/objectstack#14547 的平台修复删除。
